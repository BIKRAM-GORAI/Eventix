cd backend
npm init -y
npm install express mongoose dotenv cors
npm install nodemon --save-dev
npm install bcrypt
npm install qrcode nodemailer multer pdfkit
npm install razorpay


🧾 Final Package List (What you just installed)
express → server
mongoose → MongoDB
dotenv → env variables
cors → frontend connection
nodemon → auto restart
bcrypt → password hashing
qrcode → ticket QR
nodemailer → email
multer → file upload (profile photo)
pdfkit → certificates/tickets
razorpay → payment (demo)